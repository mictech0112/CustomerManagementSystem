<configuration>

    <!-- キャッシュの有効化 -->
    <settings>
        <setting name="cacheEnabled" value="true"/>
    </settings>

    <!-- ログの有効化 -->
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>

    <objectWrapperFactory type="org.apache.ibatis.reflection.wrapper.OptionalObjectWrapperFactory"/>

</configuration>